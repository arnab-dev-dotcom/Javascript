export const add = (a, b, c) => {
  return a + b + c;
};

// math.js
function multiply(a, b) {
  return a * b;
}

let name = "Arnab Chowdhury";

export default { multiply, name };//here export as object
//export keyword help to export the function
